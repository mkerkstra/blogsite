# Wait 8 days after a version is published before installing it.
# Mirrors the `cooldown: default-days: 8` in .github/dependabot.yml so
# local installs and CI resolve the same set of eligible versions.
# Value is in minutes: 8 * 24 * 60 = 11520.
minimumReleaseAge: 11520
